# 更新记录（CHANGELOG）

> 只记**对使用者有意义**的变化。规则级细节与依据见 [`references/rules.md`](references/rules.md)
> （每条规则都带「规则 + 依据（日期/来源）」），写法与句式见 [`references/prompt-templates.md`](references/prompt-templates.md)。
> 分发口径：仓库只放代码；大件与工作台 exe 走 [Release 附件](https://github.com/heronbo111/HeronBo-AIGC-Prompt/releases)，
> 拉齐用 `python tools/deploy.py vendor --fetch --yes`。

## v0.3.1 · 2026-09-23

**🐞 修**
- agent 弹窗两处标签（用户截图打回）：「豆包工作」装了却被归到「本机没装的」里 → 现在分成
  **「装了，但还没接通」**与**「本机没装的」**两组；右侧状态从"有，但没配命令"改成**"装了，未接通"**
  （它有 CDP 桥，只是还没接管），并给这类行补上**「测一下」**——按提示把客户端带调试端口重启后，
  当场就能重检，不必重开弹窗。
- **豆包桥改成通用发现**（换台机器也能用）：CDP 端口**自动挑空闲的**（9333 起，被占就往后找，并记住
  实际用的那个）；会话目录按 **profile 通配**（企业版/多开时不一定叫 Default）；客户端 exe 多路径兜底
  （各盘 + Program Files + 用户级安装），也可在 `agent_bridge.local.json` 写 `"doubaowork"` 指定。

## v0.3 · 2026-09-23（豆包工作接入）

**✨ 新增功能**
- **工作台能叫「豆包工作」干活了**（企业统一要求用的那支桌面端）。它**没有命令行**，工作台用
  CDP（Chrome 调试协议）接管：带调试端口把客户端拉起来 → 往聊天框灌提示词 → 从它自己的会话轨迹里
  流式读进度与答复。首次接管前需要**先退出豆包工作**（或在命令行跑
  `node tools\doubao_cdp.mjs restart --yes` 让它带着端口重启）。
- **动作流接上豆包两兄弟**：`豆包` 与 `豆包工作` 的干活过程（读哪个文件、跑什么命令、说到哪一步）
  现在能显示在工作台的执行记录里。修了旧通道的一个错：它原来指向 `~/.doubao`（本机根本没这目录，
  一条正文都读不到），真实位置是桌面端的 Electron profile（`%LOCALAPPDATA%\...\agent_mode\...`）。
- **安装向导多两个位置**：`豆包`（`~/Doubao/skills`）与 `豆包工作`（它的 `.user_skills`）都能选，
  照旧可以给其它 agent 建 junction 一份实体多处同步。

**🔧 说明**
- 豆包工作在"在跑但不是调试端口起的"时候，工作台会明确告诉你该怎么办（先退出／带端口重启），
  不会悄悄失败。

## v0.2 · 2026-09-22（第一个对外正式版）

> 版本号从这一版起按 **0.x** 算（0.x = 还在快速迭代、结构可能变；对外推广的第一个正式版）。

**✨ 新增功能**
- 工作台现在能**自己发现并装上你新推的版本**：标题条右侧多了「检查更新」（旁边显示当前版本号）。
  点开就看到「当前版本 / 最新版本 / 要下多少 / 更新说明」，点一下「下载并重启」就自动下完、
  校验、换位、重启——不用再手动拿新包覆盖（也顺手解决了"开着工作台替换会失败"的老问题）。
- 更新**保留最近两个旧版本**，弹窗里有「回滚到上一版」，换了不满意一条命令回到旧版。
- 装好后就**不再需要你记得去查**：开窗 5 秒后台悄悄查一次，有新版本才在按钮上点一个小圆点
  （不打扰、也不会自动装）；顺手会告诉你技能里的规则/模板落后了几个提交。
- 报的是**版本号**了：以前"我这台是哪一版"只能靠 exe 修改时间猜，现在界面和命令行
  （`python tools\版本.py`）都能直接读出来。
- agent 通道改成**扫盘发现 + 真测才算接上**：通道列表只列这台电脑上真有的（别的机器装了什么，
  这边不会凭空多出来）；没验证过的显示黄点，点「测一下」几秒钟确认（免费的版本自检）；
  还可以自己加任意 agent（opencode / Hermes / 自研 harness）——在 `agent_bridge.local.json`
  里写一条命令行即可，**不需要改代码**。

**🐞 修复问题**
- 修复「只装了 WorkBuddy，通道列表却把别的 agent 都显示成可用」：以前只要**盘上找到文件**就算可用，
  于是卸载残留的 npm 壳、`npx` 缓存里的包都被当成"装了"；现在要真跑通才算，且没验证过的会明确标出来。

**🚀 优化改进**
- 下载新版时**Gitee 优先、自动换源**（实测本机 GitHub 直连拿不到数据），断流可续传，
  文件校验不过就**拒绝安装**并说明原因——不会装出一个坏包。

## v2.9 · 2026-09-21

**✨ 新增功能**
- 一个项目里做多版提示词时，**每一版现在是一套完整快照**：自己的素材夹（`素材/<版本名>/`）、
  自己的正文、自己的上传夹（`平台上传/<版本名>/`）。在工作台③栏点「＋」就能开新版，
  并可以勾选**沿用上一版的哪些素材**（勾中的会复制进新版素材夹）。
- ③栏新增「版本」区：下拉选版 ＋ 时间线；切版后②栏素材与③栏正文一起换。下面还会列出
  **这一版的 @ 引用对照**（哪个编号指的是哪件素材），对不上的会标出来。
- 新增规则 71：**`@图片1 / @视频1 / @音频1` 只在本版素材内编号**；上一版的素材与正文
  只作「系列参考」（用来对齐形象 / 道具 / 台词风格口径），**不许**写进本版提示词的引用、
  也不放进本版上传夹。

**🐞 修复问题**
- 修复工作台「动作流」**可能显示别的对话的过程**：同一台机器上还有别的对话在跑时，
  它会去读"最新被写过的会话文件"，于是把**别人在干什么**显示成这一轮在干什么
  （实测看到的是另一个对话的思考与工具调用）；现在改成只认**本轮这个 agent**。
- 修复动作流**只给一行摘要**的问题：思考与说明现在**整段带换行**显示（折两行，点一下展开），
  终端命令也放宽到 220 字——此前思考只取首行 120 字，看不出它怎么判断的。
- 修复同一项目做多版时**素材会串台**的问题：以前素材与引用编号都是项目级唯一的，
  同一个 `@视频1` 在两版里可能指到不同文件；改某一版时还会拿错驱动素材——已经静音 /
  已转深度片的那件读不到，只能拿原片顶上（白做一遍、还会二次转码掉画质）。
- 修复②栏把**产物与文稿**当素材列出来的问题（`文案/` 里的口播稿、提示词曾被当成"待识别"）。

**🚀 优化改进**
- ②栏上传的素材现在会落到**当前版的素材夹**里：新建版本后新素材自然归到新版，不用手动分。
- 删除素材时也会一并清掉**当前版上传夹**里的那份副本（以前只看上传夹根目录）。
- 旧项目**零迁移**：没建过版本的项目仍按原来那套跑（素材在 `素材/`、正文在 `文案/`），
  ③栏会把它显示成「默认版」。

## v2.8 · 2026-09-21

**✨ 新增功能**
- 工作台「动作流」改为**按环境自动适配**：本机装了哪家 AI 工具，就自动读哪家的过程
  （内置 ZCode / WorkBuddy / Claude Code / Codex / Gemini / 豆包 六路）。换机器、换工具
  **不用改代码**——在 exe 旁边或 `~/.heronbo/` 放一份 `agent_channels.local.json` 指路即可；
  界面上会写明「动作流来自 X」。
- 新增规则 67「**已经静音过的素材不重复静音**」：开工先探一遍音轨，没有音轨或平均音量极低，
  就直接当静音素材用，不再白跑一遍 ffmpeg。

**🐞 修复问题**
- 修复动作流**一直是空白**的问题：此前它在所有通道上都读不到过程，进度区只剩粗粒度的输出行；
  现在思考 / 读取 / 终端 / 写入 / 要你确认都会逐条出来。
- 修复发布流程可能**漏掉中文名文件**的问题（此前这类文件有被静默跳过的风险）。
- 修复「素材已经手动静音好了、还要被重复静音一遍」的问题（二次转码会掉画质、白等一轮）。
- 修复打包版里「提示词体检」按钮静默失效的问题。

**🚀 优化改进**
- 工作台改用**固定端口**：重启后主题、侧栏收起、提示音开关这些设置不再被清空。
- 动作流每推进一个阶段会留一行「第 N 步」，回看时能看到每一步的起止。
- 端口占用改成主动探测（Windows 上同一端口能被 bind 两次且都「成功」，靠报错判断不可靠）。

**📄 文档**
- 新增 [`docs/动作流通道.md`](docs/动作流通道.md)：换机器 / 换工具时怎么把动作流接上（3 步）。
- 新增 `tools/agent_channels.example.json`：可直接复制的通道配置模板。

## v2.7 · 2026-09-21

**提示词写法**
- 新增规则 66「替换类写法升级：**毫秒逐拍 + 身份锁 + 形象参考边界 + 收尾再锁**」，
  并在 `prompt-templates.md` 第 0 节给出可直接抄的句式。
  其中「形象参考边界」是我们自己反复翻车的地方：素材图常是人物在别处的照片或白底商品图，
  不写这句，模型会把**原图背景与站姿**一起搬进成片。
- `tools/提示词体检.py` 新增两项检查（**注意**级）：`替换类·形象参考边界`、`替换类·毫秒逐拍与保持句`；
  历史稿件会吃到这两条注意，按规则 66 补句即可消掉。

**更快**
- 出提示词那一轮**只写一遍正文**：新增 `tools/project_core.py --assemble`，
  元信息 3 行与上传行由程序拼（省每轮 1–2k 输出 token，也消掉"两份写得不一样 → 体检不合格 → 返工"）。
- 「核对归类」这一轮收窄成**只解析归类**：不读技能文档、不写文件、回执由程序写。

**文档与协作**
- `AGENTS.md` 重整：新会话路由与命令前置、目录地图、`✅/⚠️/🚫` 三层边界、上下文纪律。
- 新增 `CONTRIBUTING.md`（人类贡献政策与四条硬红线）、`tools/AGENTS.md`（目录级约定）、PR 模板、
  `.claude/CLAUDE.md` 与 `.github/copilot-instructions.md` 两个薄壳入口。
- 提醒：**工作台源码不再随本仓库分发**（走 Release 附件），本仓库只放代码。

## 2026-09-19

- 新增 7 条规则：复用现成素材（别重复加工）、正文优先（先让用户能复制）、产物与文稿归位、
  素材加工要单独报一步、音色参考裁 6–10 秒、归类只管归类、提问纪律（先按经验自己定）。
- 协作约定加「维护纪律」：**口径类改动落 skill 层**，不写死在工具源码里。
- 仓库历史整理并减重：工作台源码移出公开仓库、归档瘦身；老克隆需先对齐再协作。

## 2026-09-18

- **交付形态改为「段落式」**：正文一整段（首尾括号）、**每个素材只 @ 一次**；11 节骨架降为内容清单。
- 新增「细节密度」下限：每句均字、有台词必须有神态/微表情（体检器新增对应检查）。
- 成片评分新增「台词」维度（无问题 / 错乱 / 音色不对）。
- 工作台把 agent 的动作流搬进进度区（任务可视化）。

## 2026-09-17

- 新增规则 56「**白模转绘**」：白色关节素体人偶 + 纯绿幕，比深度片更干净的动作载体；含输入视频 ≤15s 的分段口径。
- 工作台：进程可视化、真正能刹住的暂停键、项目级「不许额外加工」开关、②栏分镜字段六栏、素材仓。
- **分发方式定型**：仓库只放代码，大件与 exe 走 Release 附件（`python tools\deploy.py vendor --fetch --yes`）；
  同一命令也是"更新工作台"（exe 会比对版本自动换）。

## 2026-09-16 及以前

- 建立统一骨架与交付格式，出完跑 `tools\提示词体检.py`（外形可机器检查）。
- 工作台成型：独立窗口（pywebview）+ 多 agent 桥（ZCode / Codex / DSH / WorkBuddy）+ 样本库项目管理与评分。
- 拆解能力：读片三问 + 机器证据（`tools\成片拆解.py`）+ 拆解报告；深度片、白模、人物遮罩等素材工具。
