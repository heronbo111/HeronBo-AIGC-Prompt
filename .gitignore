*.mp4
*.mov
*.webm
*.wav
*.png
*.jpg
# 例外：工作台界面左上角要用的图标（不放行的话用户 clone 下来 logo 就裂了）
!tools/workbench/logo.png
素材/
成片/
废片/
评价/
# 例外：samples/ 下的评价 json 属于规律数据，随仓库同步
!samples/**/评价/
!samples/**/评价/*.json
# 本机私有取值/账本（不进仓库，避免多机冲突）
references/*.local.md
references/*.local.json
# 本机专有的说明/笔记（同 references/*.local.md 的口径：只在本机看，永不入库）
# 例：tools/改工作台并打包.local.md —— 工作台源码怎么改、怎么打包换位的本机手册
tools/*.local.md
# 联调通道的本机覆盖（node / CLI 路径），不写进仓库
tools/agent_bridge.local.json
# 打包后这份配置就写在 exe 旁边（本机私有：选了哪个 agent）
tools/dist/agent_bridge.local.json
# 工作台的界面偏好（项目列表排序/名次），本机私有
tools/workbench.local.json
tools/dist/workbench.local.json
# 动作流通道的本机覆盖（换机器/换工具时指路：去哪找该工具的会话正文），本机私有
tools/agent_channels.local.json
tools/dist/agent_channels.local.json
# 换位时留的旧 exe 备份（本机回滚用，别把 18MB 分发进仓库）
tools/dist/score-tool_*.exe
# Python 缓存（诊断脚本 import 工具时会生成）
__pycache__/

# exe 打包中间产物（不进仓库）
tools/build/
# 同上，但这是**旧命令**的输出目录名（`--distpath dist_stage --workpath build_stage`）。
# 2026-09-21 实测：有人按 README 里那条旧命令打包，就凭空多出两个未忽略的目录，
# 里面是 18MB 的 exe —— 谁 `git add -A` 一下就会把 18MB 塞进仓库。文档已改，这里再兜一层。
tools/dist_stage/
tools/build_stage/
# 仓库自带的 ffmpeg：**只提交压缩包**，解压出来的 bin/ 是本机派生物，不进仓库
tools/_vendor/ffmpeg/bin/
# exe 换位暂存：工作台开着时 exe 被占用，先打到这里、等它关掉再换过去
tools/_stage/
tools/*.spec
# exe **也不进 git**（2026-09-17：历史里 47 个版本共 762MB，把 Gitee 的 1GB 配额撑爆了）。
# 分发走 GitHub Release 附件：`python tools/deploy.py vendor --fetch --yes` 会把它和离线大件一起拉下来；
# 想自己打包：python -m PyInstaller --noconfirm score-tool.spec
tools/dist/score-tool.exe
tools/dist/score-tool_*.exe
# exe 的版本戳（跟远端比对用）与下载中的临时文件：随机器不同，不进 git
# ⚠️ 下面两行**不能写行尾注释**——gitignore 不支持行内注释，会把注释当成模式的一部分
tools/dist/score-tool.version.json
# 工作台「检查更新」的运行时产物（2026-09-22）：回滚用的中转副本、ASCII 名旧备份、
# 换位助手留下的日志（助手本身是 tools/更新助手.py，那份要进仓库）
tools/dist/score-tool.rollback.exe
tools/dist/score-tool_old_*.exe
tools/dist/score-tool.update.log
tools/dist/score-tool.update.cmd
tools/dist/_wtest.tmp
# 版本身份（2026-09-22）：打包/发行时由 tools\版本.py 生成（记版本号 + exe 哈希），
# 打包换位.py 会在打包前自动写一份 —— 是本机生成物，不进仓库
tools/_version.json
tools/dist/*.exe.new

# 离线大件也不进 git（2026-09-17：历史里这几样共 294MB + exe 762MB，把 Gitee 1GB 配额撑爆）。
# 分发走 GitHub Release 附件：python tools/deploy.py vendor --fetch --yes
tools/_vendor/wheels-heavy/
# 2026-09-22：核心轮子目录（pywebview 依赖 + cv2 + numpy，共 52MB）也不能进 git；
# 拆出来的能力包轮子同理。不忽略的话，`git add -A` 一下就是 52MB 进历史。
tools/_vendor/wheels/
tools/_vendor/wheels-stt/
tools/_vendor/wheels-depth/
tools/_vendor/ffmpeg/
tools/_vendor/models/
# 打好的发行附件（约 253MB）：只用 tools\发布发行附件.py 上传，不进仓库
tools/_release/
_stage/

# ── 工作台源码：不进公开仓库（2026-09-19 用户裁定）──────────────────────────
# 界面 / 本地服务 / 启动器 / 打包配置都在私有仓库（HeronBo-Workbench）；
# 公开仓库只放技能本体（SKILL.md + references/ + 文档 + 其余工具链）。
# 本机开发照旧在同一个目录里改；改完打包用 tools\打包换位.py（一条命令：验包 + 安全换位），
# 归档到私有仓库用「私有仓库目录\同步.py」（单向：技能目录 → 私有仓库）。
# 注：tools/工作台.ico 故意留公开——公开的部署.py 要用它做快捷方式图标。
tools/workbench/
tools/workbench_server.py
tools/工作台.py
tools/score-tool.spec
tools/build_exe.cmd
tools/打包换位.py
