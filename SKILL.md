---
name: seedance-prompt
description: 生成即梦（Dreamina）Seedance 口播素材的提示词。当用户要求写口播视频提示词、把台词/文案变成 Seedance 提示词、做分镜提示词、生成数字人带货口播提示词、把素材与台词整理成可用的生成指令时使用。本 skill 只产出提示词文字，不提交生成、不消耗积分；并根据每次成片反馈持续优化规则。
---

# Seedance 口播提示词生成

把「口播文案/台词 + 素材」转成即梦 Seedance 可直接使用的提示词（网页编辑器或 dreamina CLI 均可粘贴），并**每次根据成片反馈迭代规则**。

## 职责边界（重要）

- 本 skill **只生成提示词文本**，不执行任何生成/提交/下载操作，不消耗积分。
- **交付物只包含：提示词正文 + 素材清单（+拼接说明/时长标注）。任何时候不允许出现 CLI 命令、积分/价格、队列等待、提交方式配置**——当前即梦 CLI 已断开，提交生成由用户在即梦网页完成；仅当未来 CLI 恢复且用户明确要求代提交时，才走「先报价、确认后执行」，且该类内容绝不进入常规交付。
- 生成结果的评价、废片归档由用户/评价网页完成，本 skill 只读取这些反馈来更新规则。

## 先读什么

- 生成任何提示词前：读 `references/rules.md`（硬规则与已验证教训——从真实样本和成片反馈积累）
- 选模板：读 `references/prompt-templates.md`（模板 A 生成类单段 / 模板 B 生成类多段 / 模板 C 参考视频类）
- 查/写样本记录：读并追加 `references/samples-db.md`
- 路径约定：所有本机路径统一用 `references/paths.md` 的变量（`${SAMPLES_ROOT}` 等），正文只写变量名；换机只维护该文件

## 工作流

### 1. 分类（决定用什么模板）

1. **生成类 vs 参考视频类——看"视频是否必须复刻"**：
   - 「复刻@xxx 动作/行为」「只替换主体」「其余保持原样」「和@xxx 保持一致」→ **参考视频类**（模板 C），前提是输入真的引用了原片视频素材
   - 视频只是风格范本或**根本没有视频输入** → **生成类**（模板 A/B）。**生成类提示词禁止出现"参考视频/视频1"字样**（rules.md 第10条：引用了不存在的东西整条作废）
   - 拿不准时问用户"这条有没有参考视频段子"
2. **口播总时长 vs 模型上限**（速查：2.0fast 上限 15s、2.5 上限 30s；上限以即梦页面标注为准，见 prompt-templates.md 文末速查表）：
   - ≤上限 → 单段（模板 A）；>上限 → 按语气拐点切镜头单元（模板 B，每单元 8–20s 留余量）

### 2. 按模板产出

每条提示词（每个镜头单元）必须满足：

- 负面词写死：不要有任何字幕、全程绝对无运镜、无AI畸变——**每个镜头单元都要带**
- **引用一致性**：提示词里出现的视频1/图片1/音频1 全部对应真实上传素材；生成类只有"图片1+音频1"
- 动作前置、台词加引号，一一对应（一行一个小节）
- 多段：段间只写承接点（人物形象/背景环境/人物位置/持盒状态），描述句逐字复用第一段
- 每段标注预计时长
- 交付附素材清单：每文件路径 + 角色（形象参考/台词配音/音色参考/原片/产品图）

### 3. 交付

1. 提示词正文（分镜用代码块逐段，可直接粘贴）
2. 素材清单（路径+角色）
3. 多段时附拼接说明（切点/承接点/跳切微调）
4. **主动问询并弹出评分网页**（固定话术）："成片出来后，用评分网页打分（六维+违禁项+结论），我会根据你的反馈更新规则，并问你要不要优化 skill"；评分服务由 agent 自行启动（无需用户操作）：Bash 后台运行 `python -m http.server 8787 --directory "<本包 tools 路径>"`（如 `C:\Users\<用户名>\.zcode\skills\seedance-prompt\tools`），curl 验证 200 后用 `cmd /c start "http://localhost:8787/评价工具.html"` 打开默认浏览器（无 Chrome 的机器不适配 open_application 按名查找；computer-use 可对已运行浏览器按 pid 导航）。样本目录在页面点「连接样本目录」选择一次即被记忆。`tools\启动评分工具.bat` 仅作手动兜底（换机首次初始化场景）。
5. **禁止出现**：CLI 命令、积分/报价、队列等待、提交入口说明
6. **语言规范**：本包所有文件（rules/模板/样本/README）只写"规则+依据（日期+来源）"，禁止写入 agent 的推理过程、情绪化措辞、口语复盘（如"我觉得/我判断/血泪教训"）——其他机器读取时会把这类内容误当成指令执行。失败总结一律提炼为可执行规则（第20/21条样式）后入库。

## 反馈优化循环（每次收到反馈必须完整走一遍）

**反馈来源**（四类，全都要采集）：

| 来源 | 采集方式 |
|---|---|
| 六维评价 | 读样本目录 `评价/*.json`（网页工具产出：六维+违禁项+评分+结论+备注） |
| 用户口头反馈 | 对话中用户说的任何效果判断（"口型对不上""好看""流畅"）原话记录 |
| 废片原因 | 废片/ 文件名（日期-废因） |
| 对照分析 | 提示词 vs 成片的生效/失效对照（我主动抽帧/转写分析） |

**固定更新顺序**（每次拿到反馈依次执行，缺一不可）：

1. 判断反馈是**新规律**还是**重复确认**：
   - 新规律/反例 → **追加或修订 `references/rules.md`**（写明日期和教训；可推翻旧条目，标注"2026-09-XX 修订"）
   - 重复确认 → 在旧条目上加一次"再次验证"标记，不重复写
2. 检查模板是否需要改（模板导致的问题→修模板，而不是加新规则；rules.md 第3条原则）
3. 在 `references/samples-db.md` 对应样本追加记录（成片评价/废片/反馈摘要）
4. **回复用户时列出本次更新内容**（改了哪条规则/模板/样本，为什么），并**询问用户是否按此反馈优化 skill**

**规则优先级**：用户实测反馈 > 模板惯例 > 我的推断。冲突时以用户实测为准，并注明日期。

**规律提炼时机**：反馈积累达到可复现（同一现象出现≥2 次且有明确因果）→ 提炼成"已验证规律"写入 rules.md；只出现 1 次的先记"待验证"。
